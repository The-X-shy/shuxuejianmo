# 多模态情感建模：逐步实验方案与执行顺序

版本：1.0｜日期：2026-09-23｜配套约束：多模态情感建模_执行Spec.md v1.0

## 阅读说明

**Spec 规定哪些事情允许做、结果必须满足什么条件；本文规定从收到数据到交付结果，每一步具体怎么做。** 本文不替代 Spec，尤其不改变数据来源、划分、标签定义、专项测试用途和提交要求。

本文是待执行的实验手册，不是已经完成的实验报告。当前有题面，没有附件1–4；没有训练结果。文中的操作、产物名称、参数和检查标准供后续执行使用，软件版本以实际安装成功后的锁定记录为准。

配套文件：

- `实验默认配置.json`：供 agent 实现项目时读取的参数合同，不绑定尚未编写的某个训练程序。
- `核心实验运行队列.csv`：按实际顺序排列的24次训练，所有状态初始为 PLANNED。
- 原 `多模态情感建模_执行Spec.md`：遇到边界问题时的判断依据和最终验收标准。

**不要一开始执行24次训练。** 先完成数据核验、掩码验证、人工指标样例和32样本调试；这些通过后再进入正式队列。问题1的批量提取与问题2/3训练互不依赖，可以分开执行。

## 一、执行顺序总表

| 顺序 | 操作 | 开始条件 | 完成后获得什么 |
|---|---|---|---|
| E00 | 建立实验记录与两套环境 | 题面、Spec | 环境清单、预算、实验登记表 |
| E01 | 核对附件、ID、字段与时间映射来源 | 实际附件到齐 | 数据审计、标签映射、长度依据、Q4映射可行性结论 |
| E02 | 转换数据、区分掩码、计算训练集统计量 | E01相关数据通过 | 可以直接训练的数据、固定统计量 |
| E03 | 生成固定缺失场景和抽检样本 | E02 | 90主场景、15压力场景、异步场景、固定核验清单 |
| E04 | 验证指标和模型，完成32样本调试 | E02/E03 | 正式训练前的正确性报告和单轮计时 |
| E05 | 对5条视频试跑问题1全流程 | E01、提取环境可用 | 可核验的三模态时间对应与提取耗时 |
| E06 | 完成问题1全部100条 | E05 | 全量特征、汇总表、至少1张对齐图 |
| E07 | 运行seed=42的8个核心模型 | E04 | 第一轮完整对照，暴露程序或数据问题 |
| E08 | 补seed=17、2026 | E07无未解决正确性问题 | 每个模型3次训练，共24次 |
| E09 | 计算完整评价、缺失规律与配对比较 | E08 | 选型依据、性能表、规律曲线 |
| E10 | 按预算尝试可选增强 | E09，且基础任务有充足完成时间 | 保留或否决重建/一致性的证据；可直接跳过 |
| E11 | 用验证集检查解释并确定解释规则 | E09或E10选出候选 | 数值解释验证、耗时、失败案例 |
| E12 | 固定最终模型与全部规则 | E01–E11所需条件通过 | 冻结记录、唯一提交权重 |
| E13 | 评估附件2 test | E12 | 一次独立补充评价，不再选模型 |
| E14 | 推理附件3/4，完成Q4原视频证据 | E12、Q4映射依据可用 | 两份全量CSV、解释明细、解释卡 |
| E15 | 整理图表、复现检查、打包 | E06/E13/E14 | 论文素材、≤50MB提交包、验收记录 |

单人顺序：E00→E01→E02→E03→E04→E05→E06→E07→…→E15。

多人顺序：E01后，数据提取成员负责E05/E06；模型成员负责E02–E10；解释成员核验Q4映射来源并准备展示格式，待模型候选产生后执行E11，冻结后执行E14。相同GPU上的重任务默认排队，不能把逻辑并行当作资源无限。

## 二、逐步操作

### E00｜建立实验记录与环境

**目的：** 每个结果都能找到对应的数据、代码、参数和运行过程。

1. 创建 `data/raw/`、`data/processed/`、`manifests/`、`configs/`、`runs/`、`results/`、`submission/`。原始附件只读保留，派生文件不得覆盖原始文件。
2. 将 Spec、本文及配置复制到项目文档目录，记录版本与SHA-256。填写实际机器的系统、CPU、内存、GPU、显存、可用磁盘空间。
3. 建立两个环境：模型环境只安装数据读取、PyTorch、评价、绘图所需库；提取环境安装FFmpeg、OpenFace、openSMILE、BERT和CTC工具。默认Linux/Python3.10；不因提取工具安装问题阻塞已可开展的数值特征训练。
4. 记录实际安装版本、开源仓库commit、预训练权重revision/校验值、设备及运行命令。先用单样本确认依赖可加载，再固定环境。
5. 为每次训练登记 `run_id、experiment_id、seed、config_hash、data_hash、code_commit、start/end、status、best_epoch、failure_reason`。运行结果只能写入自己的目录。
6. 使用配套队列，默认只安排24次核心正式训练；调试、计时和失败重跑单独登记，不混入成绩。

**输出：** `environment_model.json`、`environment_features.json`、`experiment_registry.csv`、`budget.md`。

**通过条件：** 一条虚拟数值样本可以完成模型前向；一条实际视频能被读取。提取环境失败只阻塞E05/E06，不应使模型环境回退或改变。

### E01｜核对数据与时间映射来源

**目的：** 确认数据真能支持后面的实验，而不是训练完成后才发现解释无法定位。

1. 逐个文件登记名称、大小、校验值和附件编号。附件1以 `(video_id,clip_id)` 与表格做外连接，列出缺文件、多文件、重复ID与标签冲突。
2. 读取附件2字段清单，分别打印train/valid/test的样本数、三模态shape、dtype和标签字段长度。核对总量4850，不预设各划分人数。
3. 对 train/valid 核验三类计数、连续标签最小/最大值、y=0数量。逐条对照文本标签、整数标签和连续标签符号；冲突写出ID与原值，不自动重标。
4. 结构检查可读取test的ID、shape和完整性信息，test标签和评价统计不提供给开发过程。附件3/4只检查格式、对应关系和必要映射元信息，不统计其预测表现用于选择方案。
5. 检查跨划分重复ID及相同内容，报告共同video_id；保留官方划分。真有同一建模样本重复且影响独立评价时，记录问题并取得解释，不静默删除或混合重分。
6. 为有效长度列出证据：明确长度字段、经核实的attention mask，或官方预处理说明。检查 `[CLS]/[SEP]`、词元合并与尾部填充是否影响三模态行对应。
7. 抽查附件2的20条train/valid样本：包含长/短、各极性和出现零行的情况。输出三个模态的零行位置图，对照长度说明；抽查不是替代全量自动检查。
8. 对附件4建立 `sample_id → feature_row → video_path → transcript/token_source → position_mapping_source`。区分直接时间表、可重建映射和无法核验。冻结前允许验证这些来源与结构，不计算模型预测后据此调参。

**输出：** `input_manifest.json`、`data_audit.json`、`label_mapping.json`、`length_evidence.md`、`q4_mapping_inventory.csv`、`issues.csv`。

**通过条件：** 模型数据的ID/维度/标签/有效长度规则可解释；Q4映射至少有明确的可行或阻断结论。

**失败处理：** 缺附件不造数据；长度歧义不一律设为50；Q4缺时间依据不假设等长时间窗。只暂停依赖该信息的步骤，并把需要补充的具体文件或说明写入issues。

### E02｜数据转换、三个掩码与标准化

**目的：** 让模型能区分真实位置、可见信息和人工删除。

1. 按官方原顺序保存train/valid/test的三个float32数组、ID、标签和元信息。`text`是主输入，`raw_text/text_bert`进入独立核验文件，不传入模型。
2. 生成 `P[i,t]`：位置有效；生成 `O0[i,m,t]`：原始输入该模态有观测。先读取原始值再判断整行全零，不能标准化后才判断。
3. NaN/Inf写入异常表。默认将含非有限值的该模态整行标为invalid、O0置false、数值占位为0；此规则在train/valid审计阶段固定，独立于题面人为置零缺失统计，不删除样本。
4. 只在train中 `P & O0` 的位置逐维计算均值μ和总体标准差σ（ddof=0）。零方差维分母设为1；某一整维在train没有任何可观测值时，停止并核查，不能从valid补统计量。
5. 全部划分使用相同μ/σ。预处理采用条件赋值：可观测位置为 `(x-μ)/σ`，其他位置为0。不要用 `NaN×0` 试图清除NaN。
6. 人工遮挡时生成C，满足 `C ⊆ P & O0`；本次可见掩码 `O=O0 & ~C`。人工遮挡后重新把O=false处设0，再送入模型。P不随C变化。
7. 保存训练集类别比例π和连续标签均值，用于完全无有效位置的应急输出；该分支必须带异常标记，仍计入全量评价。
8. 手查3个具体样本：正常短序列、存在自然零区间、人工三模态同区间删除。列出P/O0/C/O逐位置值，确认padding没有进入遮挡和均值。

**输出：** `processed/{split}/`、`normalization.npz`、`train_prior.json`、`preprocessing_manifest.json`。

**通过条件：** 数组有限；P/O0不随不同模型变化；改变valid数值不会改变μ/σ；相同原文件重新转换得到同样ID/标签/掩码和统计量。

### E03｜生成固定缺失情形与抽检清单

**目的：** 每个模型面临完全相同的缺失条件。

1. 场景名称使用整数百分数10/20/30/40/50；计算时先转为 `r=percent/100`。L为有效位置数；L>0时长度为 `ell=max(1,round(r*L))`，使用Python的round约定并在实现中记录。L=0时C全部为false，不生成起点，实际缺失比例为null并注明原因；样本保留在全部评价情形中，以训练集先验分支预测。
2. 有效位置连续时，前/中/后起点分别为0、floor((L-ell)/2)、L-ell，再映射至原始行索引。若有效位置被特殊符号等分成多个段，不跨无效位置拼接：在原索引合法段内构造区间，长度不足时记录实际长度，保留样本。
3. 生成90个主情形：T/A/V/TA/TV/AV × 五比例 × 前/中/后。双模态使用同一起点。
4. 生成15个TAV压力情形，三个模态共享同一区间。生成异步压力情形：TA/TV/AV，30%，每样本3组；两模态有条件时选不同起点，短样本只有一个合法起点则保留并标注退化。
5. 固定异步种子3407。每个随机情形使用稳定key：`SHA256(protocol_version|seed|sample_id|scenario_id|replicate)` 的前8字节按big-endian转整数作为PCG64种子。不要使用进程间可能变化的内置hash。
6. 只保存ID、被选模态、原索引起止、请求长度、实际新增缺失点与实际比例，不复制整份特征90遍。评价时逐情形即时构造输入。
7. 创建每轮早停用的6个情形：6种类型、30%、中部。必须与90网格的对应项完全相同，不能另外抽样。
8. 用同一规则保存验证解释样本清单：按真实三类分层随机抽取最多100条，seed=3407，尽量分散video_id；不足类别全取，剩余额度按其他类别样本量补齐。不得按哪个样本解释更漂亮来选。

**输出：** `valid_scenarios.jsonl`、`valid_earlystop_scenarios.jsonl`、`explanation_valid_ids.json`、`scenario_summary.csv`。

**通过条件：** 情形数90+15，异步每ID9个变体；所有C都在P&O0内；相同输入重建得到同一mask；不同训练seed共用同一验证情形。

**示例：** L=37、r=0.3，ell=11；前/中/后对应有效位置区间 `[0,11)`、`[13,24)`、`[26,37)`。这些位置比例不能写成真实秒数。

### E04｜指标核验、边界检查与32样本调试

**目的：** 把读错标签、掩码泄漏、损失写反等错误挡在正式训练之前。

**A. 指标人工样例。** 类别固定顺序为0/1/2=负向/中性/正向；评价显式包含三类，零分母F1记0并报告该类样本数。

| 项目 | 输入 | 正确结果 |
|---|---|---|
| 三分类 | 真值[0,1,2,2]；预测[0,2,2,0] | Accuracy=0.5；Macro-F1=7/18；Weighted-F1=5/12 |
| 混淆矩阵 | 行真值、列预测 | [[1,0,0],[0,0,1],[1,0,1]] |
| 回归 | 真值[-1,0,1,2]；预测[-0.5,0.5,1.5,1.5] | MAE=0.5；Pearson≈0.9438798074 |
| 常量预测 | 所有预测强度相同 | Pearson=null及原因，不填0 |

**B. 模型边界检查。** 对B3、B5、M0/M1分别检查：padding换成任意数、缺失行换成任意数但掩码不变，预测均应不变；单模态全缺、双模态全缺、TAV同区间缺失、L=1、全无有效位置均无NaN/Inf。比较时eval模式、固定设备，默认绝对容差1e-6。

**C. 32样本调试。** 从train固定取最多32条且尽量覆盖三类，不使用valid。仅对B3和M1做可拟合性检查：暂时dropout=0、关闭人工缺失，用相同小批量训练最多1000步，调试lr=1e-3。目标为训练Accuracy≥0.95、MAE≤0.15；这是排查目标，不是正式性能门槛。未达到时检查重复输入/矛盾标签、梯度、损失、头输出范围和参数是否更新，不凭一次失败认定数据不可学。

调试参数与权重全部丢弃。正式训练必须重新初始化并恢复默认配置，不能把这32条调试样本变成额外预训练。

**D. 计时。** 正式结构上以默认batch完成一个训练epoch和一次“完整+6情形”验证，另记录单批峰值内存。先预热再计时，日志中分别保存训练和验证时间；CPU回退或内存不足记录原因。

**输出：** `correctness_report.json`、`debug_fit.csv`、`timing_pilot.csv`。

**通过条件：** 人工指标与边界检查全部通过，小样本拟合能解释；计时足以估算24次训练成本。运行错误修复后重做受影响检查，不能直接进入E07。

### E05｜问题1：5条视频完整试跑

**目的：** 验证特征、音视频时钟和文本时间确实能对应。

1. 按技术条件选5条、不看情感标签：最短、最长、时长中位数附近、词元最多、音轨偏移/变帧率最明显各1条；重复时按同一条件的下一条补齐，ID排序打破并列。保存选样理由。
2. 对每条视频提取容器时长、视频逐帧呈现时间戳、音轨起点与采样率。定义视频首个呈现帧为t=0；保存音轨相对偏移Δ。聚合时长D使用视频实际呈现时段（最后呈现帧的结束时间减首帧起点），不直接使用可能含音轨额外空白的容器时长；末帧时长依据也须记录。
3. 解码单声道16kHz音频，记录实际解码后样点零点。如果输出没有裁剪或补齐，音频第n个样点对应视频时间 `n/16000+Δ`；若工具已裁剪/补齐，按实际变换记录修正，不能再重复加Δ。
4. 使用题供英文转写进行CTC强制对齐。原文原样保存；另存对齐使用的规范化文本及字符对应，大小写/标点清理不能覆盖原文。保存每个词的起止时间、失败标记和工具返回的质量信息，工具没给置信度时不编造。CTC词时间与后续LLD时间都按第3步同一换算转换到视频时钟，保留换算前后值。
5. BERT加载固定revision并eval、冻结参数；用tokenizer字符offset把子词还原到原词，词向量为该词有效子词向量平均。特殊词元不分配发音时间。超过模型最大内容长度时按完整词分块，保存每块原始字符范围，不截掉尾部。
6. 提取25维eGeMAPSv02 LLD，保存帧时间；OpenFace提取Spec指定29列与success/confidence。OpenFace输出行须与解码帧顺序匹配，再回到真实呈现时间戳。
7. 将视频划为50个等长时间窗。第j窗的文本向量为 `Σ overlap(word_i,bin_j)*h_i / Σ overlap`；无交叠时文本占位0。只有依据回听或已核验的语音活动记录确认没有语音时才标no_speech，并记录依据；存在未对齐词、转写不完整或原因不明时标alignment_unavailable，未知词时间保留null。LLD按转换后帧中心时间入窗求均值；视觉对success=1且confidence≥0.8的帧入窗求均值。
8. 生成一张三模态对齐图：原文与词时间、音频波形、对应帧、50窗边界；任取3个可听清的词，人工回看起止时间。录入真实人工核对值，不以CTC输出复制成“人工真值”。
9. 记录单视频各环节耗时、峰值内存、词匹配率、有效人脸比例、异常窗数。

**输出：** 5条临时特征、`pilot_alignment.jsonl`、`pilot_qc.csv`、对齐图与耗时。

**通过条件：** 5条均完成处理或有可解释的素材异常；没有视频/音频时钟混用；人工核验能定位回正确内容。

**失败处理：** 程序崩溃修程序；原视频无脸/无语音才作素材异常。多人脸固定选持续跟踪时间最长的轨迹，并列选平均脸面积较大者，再并列按轨迹编号；保存轨迹切换与歧义，不能确认是否发言者时标speaker_ambiguous，不称为已确认的发言人视觉证据。词对齐失败保留该词与空时间，不能假设均匀分布填满视频。

### E06｜问题1：全量100条与质量复核

1. 锁定E05通过的工具版本、列顺序、时钟规则、目标脸选择和50窗规则，按固定ID顺序处理100条，每条单独保存状态，以便中断后续跑。
2. 缓存key包含原视频、转写、模型revision、配置hash。只有key完全一致才能复用；改了规则就重做受影响样本，不能混合新旧版本。
3. 聚合得到 `[100,50,768]`、`[100,50,25]`、`[100,50,29]`，保存全部窗口状态。文件中清楚标记自提版本，禁止送入官方特征训练的模型。
4. 自动检查所有时间区间合法、样本ID唯一、原标签未修改、特征无NaN/Inf；检查每条50窗并集覆盖其全部定义视频时间。
5. 预先按seed=3407抽取10条作人工对齐核验，每条至少3个清楚词边界，覆盖多个video_id；失败与不可听清部分也记录。计算边界绝对误差，建议中位数≤0.2秒；不达标时定位偏移/词漏配原因并修正受影响流程。
6. 输出100行样本表，另存300行“样本×模态”详细表。正文汇总使用全部100条，不以抽查替代全量结果。

**输出：** Spec规定的 `q1_features.npz`、`q1_feature_schema.json`、`q1_inventory.csv`、`alignment_q1.jsonl`，以及 `q1_manual_check.csv`、对齐图。

**通过条件：** 100/100均有记录；所有未解决程序错误清零；自然异常按规则保留并说明。正常样本处理失败不能用全零冒充成功。

### E07｜第一轮正式训练：seed=42

**目的：** 用8个明确对照回答“多模态、缺失训练、时间建模和动态权重分别有没有帮助”。

**执行顺序：** B3→B4→M0→M1→B5→B0→B1→B2。优先四个成对模型，尽早确认主线有效；仍须完成全部8个，不能只留下有利对照。该顺序已写入配套队列。

| 模型 | 本轮做什么 | 重点比较 |
|---|---|---|
| B3 | 三模态可见位置均值→拼接→两层128维MLP | 最简单多模态起点 |
| B4 | B3加固定的缺失训练协议 | B4对B3：增强是否有效 |
| M0 | 动态融合与两层时间模型，无人工缺失 | 与M1形成同结构对照 |
| M1 | M0加缺失训练 | M1对M0：同结构增强收益 |
| B5 | 与M1相同，但只对可见模态等权融合 | M1对B5：动态权重收益 |
| B0/B1/B2 | 文本/声学/视觉单模态均值→两层128维MLP | 各模态信息基础，不作为主多模态候选 |

**将结构补齐到可直接实现：**

- 每模态投影：Linear(Dm,128)→LayerNorm(128)→GELU。使用Spec定义的安全掩码融合。
- 动态门控使用三个模态共用的 `Linear(132,32)→GELU→Linear(32,1)`；132维由该模态128维表示、该模态可见比例q_m和当前位置3维观测标记组成。
- 融合后加入 `Linear(3,128,bias=false)` 的观测状态表示和固定正弦位置编码；有效但全缺失位置使用可学习128维向量。
- 时间层为2层、hidden128、4头、FFN256、GELU、dropout0.2、norm_first=true、LayerNorm eps=1e-5；最后再做LayerNorm，按P做均值池化。禁止对全padding样本直接执行全掩码attention，单独走训练先验分支。
- 分类/回归头分别为 `Linear(128,64)→GELU→Dropout(0.2)→Linear(64,3或1)`，回归再接3×tanh。基线MLP也使用这两个头。
- B3/B4输入880维；B0/B1/B2输入Dm+1维。两层隐藏层均为Linear→GELU→Dropout(0.2)，均值的分母仅计可观测位置，整模态不可见时均值为0、q_m=0。
- 同一seed下，B3/B4共享相同初始化；B5/M0/M1的共用模块从相同初始权重复制。仅相应实验使用动态门控，避免初始化差异混进对照；所有模型最终独立训练，不继承对照的已训练权重。

**每次运行前：** 检查数据与配置hash；设置Python、NumPy、PyTorch及设备随机种子；关闭drop_last；默认float32、无自动混合精度、无学习率调度器。确定性运算无法支持时记录具体算子和替代方式，不默默更换。formal epoch从1开始。

**每个训练epoch：**

1. 用独立随机源打乱train顺序。默认batch_size=32，不丢最后一个小批次。
2. 对需要缺失增强的模型，为每条样本以 `mask-v1|train|seed|epoch|sample_id` 生成SHA-256前8字节的PCG64种子，big-endian解释。此随机源独立于batch顺序、dropout和DataLoader。
3. 固定抽样顺序：训练类型→模态组合→比例→共享方式→起点。类型按概率0.30/0.60/0.10分别为不追加缺失/局部/整模态；每批无需恰好达到这些比例。局部组合七选一，比例五选一；TAV强制共享，双模态以0.5概率共享，否则独立起点。整模态从T/A/V/TA/TV/AV六类均匀抽取。
4. 从标准化完整输入复制出受损输入，更新C/O但不修改P和原缓存。B4/B5/M1同一seed同一epoch同一ID必须得到同一C。B0–B3和M0不追加人工缺失。
5. `train模式→清零梯度→前向→CE+SmoothL1(beta=1)→反向→梯度范数裁剪1.0→AdamW更新`。lr=1e-4、weight_decay=1e-4；先不启用类别权重、重建和一致性。
6. 按真实样本数加权累计损失、分类错误与缺失计数；不要直接平均不同大小批次的均值。
7. 每轮结束在eval/no_grad下，对全部valid执行原始输入和6个固定轻量缺失情形。这里“完整”只表示不追加缺失，不把原本缺失的信息补回。
8. 计算早停分数：

   `J_e = 0.5*MAE_clean + (0.5/6)*Σ MAE_{T/A/V/TA/TV/AV,30%,middle}`。

9. 第一轮有限J必保存；此后仅 `J_e < best_J - 1e-4` 记为改善，保存最佳权重并清零等待计数；否则计数+1。连续8轮无改善停止，最多60轮；并列保留较早epoch。
10. 训练结束恢复best权重，保存完整valid逐样本预测、6情形摘要、best_epoch及运行时间。每次运行最好结果不覆盖其他seed目录。

NaN/Inf立即结束该次运行并记录样本ID、模块与损失；禁止跳过异常批次继续算成功。内存不足先减物理batch并用梯度累积保持有效batch32；记录变化并核验与原设置一致性，不能让某个模型单独获得额外调参。

**每个run目录至少保存：** `config.json`、`manifest.json`、`epoch_metrics.csv`、`best_weights`、`best_epoch.json`、`valid_clean_predictions.csv`、`run_status.json`。用于恢复训练的优化器和随机状态另存，不进入最终提交包。

**通过条件：** 8个模型都能完成，最佳epoch可恢复，数据/指标/缺失协议一致；第一轮成绩只用于发现问题，不能据此只补有利模型的seed。

### E08｜补齐seed=17和2026

1. 固定E07的代码和实验配置，不搜索新的参数。
2. 按相同模型顺序先跑seed17，再跑seed2026；使用同一验证掩码和不同训练随机种子。
3. 每个seed重新初始化。不得从seed42的已训练权重继续训练然后称为新的独立seed。
4. 完成后核对8个模型各3个run，总数24。失败记录保留，修复正确性问题时给出受影响run清单并重做，禁止删掉表现差但正常完成的运行。
5. 若时间只允许seed42，明确标为探索性结果；不能把未完成的三seed稳定性分析写成已完成。

**输出：** 24行核心运行登记表、各run最佳权重与完整日志。

**通过条件：** 每个模型三个可比结果；配置差异仅为实验定义允许的项目和seed。

### E09｜完整评价、缺失规律和候选选择

1. 对每个run的best权重评价：1个完整情形、90个主情形、15个TAV压力情形、9个异步变体设置。同一模型总计115类评价视图，全部复用既有权重。
2. 每个情形保存每个样本的类别概率、强度、真值、ID、缺失信息；另存Accuracy、Macro/Weighted-F1、每类F1、MAE、Pearson与错误计数。未定义Pearson保存null及原因。
3. 每个模型对每个情形汇总3seed均值±样本标准差（ddof=1）。定义R_s为某一seed的90情形MAE等权平均，主分数R为三个R_s的平均；不将90情形当90次独立训练。
4. 按Spec筛选多模态候选：完整MAE≤B3三seed均值的1.05倍，完整Macro-F1≥B3均值−0.02。筛选后优先R更小者；R差≤0.001视为近似，比较主网格平均Macro-F1，再比较参数量。该容差在查看结果前固定。
5. 同时按相同seed配对计算B4−B3、M1−M0、M1−B5、M1−B4，明确每项比较回答什么问题。B0/B1/B2用于信息贡献对照，不因成绩偶然好而冒称完整多模态方案。
6. 基础候选没有优于B4且B4合格时保留B4；没有增强候选满足完整性能门槛时回退B3，照实说明缺失性能限制。
7. 画三组规律图：固定类型下缺失比例曲线；固定30%下前/中/后比较；固定位置/比例下不同模态组合比较。TAV和异步压力单独展示，不混入主分数。
8. 可选配对置信区间：按video_id重采样1000次，seed=3407；每次带走该视频所有片段和缺失变体，三个seed分别计算差值后取均值，报告2.5%/97.5%分位。无法取得可信video_id时标记限制，不能假装所有遮挡样本独立。
9. 从valid按固定规则整理失败：中性误判、文本缺失明显退化、全缺失区间退化、分类/回归方向不一致。报全量比例，再选若干样本解释，不能只展示成功。

**输出：** `metrics_by_run.csv`、`metrics_by_condition.csv`、`paired_comparisons.csv`、`candidate_selection.json`、曲线与混淆矩阵。

**通过条件：** 所有结论可追溯到ID与run；没有专项集或附件2 test参与候选选择；没有将缺失比例换成未经验证的秒数。

### E10｜可选增强：逐项尝试，不同时加入

默认跳过。仅在核心24次完成、问题1和解释时间已有预留时执行。

**M2：重建。** 从M1结构重新初始化，增加从时间表示到各模态原维数的线性解码器；目标为E02中保存的标准化完整特征。仅对C=true且原本可观测的位置计算MSE：各模态先除以其遮挡点数×维数，再对有监督模态平均；无目标则为0。总损失为主损失+0.1×重建损失。重建值不回填预测输入。

**M3：一致性。** 同一seed的M0最佳权重作冻结teacher，eval且不反传；student从M1结构重新初始化，读受损输入。teacher仅在train读取完整输入，温度1；一致性为 `KL(p_teacher || p_student)+SmoothL1(y_teacher,y_student)`，按batch平均，总损失为主损失+0.1×一致性。验证/推理时不运行teacher，不把其答案传给student。

**执行规则：** 先各跑seed42；用各自seed42结果检查相对于B3_seed42的完整性能限制，并比较增强模型与M1的R_42。若R_42没有至少改善0.001则不补seed，但保留负结果。通过的版本补17/2026。最终只有三seed平均R相对M1改善≥0.001、三seed完整性能限制通过且至少2/3 seed的R_s不差于同seed的M1，才保留。该判据是保守工作规则，不等于统计显著；不能将仅跑过seed42的增强列为已完成三seed对照。

只有M2/M3都保留才测试M4，两系数不变；M4同样从头训练。三个版本最多9次。额外MulT最多3次，属于预算充裕的参考对照，需要统一输入/遮挡/评价并标注适配版本；本文默认不执行。核心加全部可选最多36次，不包含另行登记的调试或参数搜索。

**失败处理：** 辅助损失下降但主任务不改善则否决；不能因为模型更复杂而保留。正式数据不足以支持结论时保留简单版本。

### E11｜解释模块与验证集检查

**目的：** 判断解释是否忠实反映模型变化，并提前测出全量解释成本。

1. 使用候选结构的seed42最佳权重，固定E03选出的最多100个valid ID。模型eval，原始输入预测只计算一次并缓存；解释前后原预测必须一致。
2. 对每样本保存原预测类c、该类概率和强度。分别移除整个T/A/V，重新计算q和观测状态，P不变。保存 `p_c−p_c(-m)`、`y−y(-m)`及其绝对值。
3. 分类作用程度按三个绝对概率差归一化；总和≤1e-8时权重全0、主模态None。并列容差固定1e-6。回归主模态独立计算，不强制两者一致。
4. 对每模态的每个可观测有效位置做单点遮挡，固定原类c不变；分批组织扰动输入，初始扰动batch32，内存不足可缩小，不影响数值定义。输出两类带符号曲线及绝对曲线。
5. 按长度min(3,L)、步长1形成合法连续候选窗。每模态以单点绝对概率差的区间和排序，同分按起点升序；贪心取最多3个不重叠窗，无可观测信息的窗不选。
6. 对选中每个窗重新联合遮挡，保存真实联合变化。别把各单点差值相加当作窗的影响。对回归结果保存同窗的变化；必要时另列回归最高窗，明确两种排序来源。
7. 同一模态为每个选中窗抽取10个随机对照，匹配长度、有效位置数和可观测位置数；seed3407+样本ID+模态+重复号稳定生成。可重合真实选中窗，不为制造差距强制避开；无可匹配窗时标不可比较，不改长度硬凑。
8. 比较联合遮挡产生的绝对概率变化/绝对强度变化；支持证据另看概率下降，反对证据另看概率上升。汇总每样本配对差、三类分组、失败比例和随机对照覆盖率。
9. 验证只说明解释对当前模型的敏感性，不证明因果正确。单点排序用单点变化，检验用联合窗变化；不能只展示“最大的单点变化比其他单点大”这种必然结果。
10. 保存耗时和峰值内存。每样本最多163次等价前向，验证随机对照最多再加90次；实际数依可观测位置和选中窗数量减少。按实测量估算附件4全量成本。

**输出：** `explanation_valid.jsonl`、`explanation_comparison.csv`、`explanation_timing.csv`、至少6张验证解释卡。

**通过条件：** 数值稳定、原预测不变、区间确由计算产生；如高敏感区间未明显优于随机对照，应报告解释局限，不能把图画出来当作有效性已证实。

**展示选样：** 在固定验证集合内，优先覆盖三类、不同主模态，至少包含一个预测错误或解释未优于随机的例子；不足的类别/类型注明不存在，按ID顺序补足。选图不改变全量统计。如果以后修改解释排序或窗口，记录新版本，并从未用于修改的valid视频组中另留审核样本；不能反复在同100条上改到“通过”。

### E12｜固定最终模型与全部规则

1. 根据E09/E10的三seed结果确定结构，提交权重固定为该结构seed42的best epoch。不得从三seed中另挑专项预测看起来最好的一份。
2. 固定归一化、掩码、类别映射、是否使用可选损失、解释窗长度、排序、模态判定容差、原视频映射规则和导出字段。
3. 写入 `frozen_config.json`：选择理由、配置hash、权重hash、数据版本、训练/验证ID清单hash、代码版本、最佳epoch、环境版本、冻结时间。
4. 正式提交只保存一份模型权重；验证该权重不含BERT/CTC缓存、teacher、优化器状态或其他seed。
5. 如Q4映射依据仍缺失，明确Q3原始证据定位尚未通过；可以固定模型并完成其他步骤，但不能把全项目标为完成。

**输出：** `frozen_config.json`、`submission/model_weights`、`model_selection_note.md`。

**通过条件：** 能唯一确定最终预测来自哪份输入、参数与规则，后续不得据附件2 test或专项结果重新选择。

### E13｜附件2 test的一次独立补充评价

1. 载入冻结参数和统计量，对附件2 test执行同一数据处理；此时才解锁test标签评价。
2. 报完整数据指标；可用相同规则追加90主情形及压力测试，全部复用冻结权重。test遮挡清单按固定规则生成并保存，不从结果中选择“代表情形”。
3. test结果只用于报告泛化与局限，不触发换结构、调整阈值、类别权重或重新训练。
4. 如发现可证明的程序错误，保存第一次结果和证据，修复后给出受影响范围；已查看的test不再声称完全未见，明确披露重算原因。

**输出：** `test_final_metrics.csv`、逐样本结果与 `test_evaluation_log.json`。

**通过条件：** 使用E12冻结版本，结果完整，无后续模型选择。

### E14｜附件3/4全量推理与证据定位

**附件3：**

1. 校验输入aligned版本、字段维度与ID，使用固定的自然缺失判定，禁止再施加训练用人工遮挡。
2. 逐批预测三类概率、类别和连续强度，保留原ID顺序；失败先定位原因，不能删去该行。全无信息按固定分支输出并标注。
3. 写 `q2_predictions.csv`，检查ID覆盖100%、类别合法、概率和为1、强度在[-3,3]且全部有限。
4. 只汇总数量、预测分布、异常数和耗时；没有标签，不算准确率。

**附件4：**

1. 读取官方特征进行普通预测，复用E11确定的解释规则逐样本生成模态和局部敏感度。
2. 使用E01核验的来源完成映射：先确认官方第k行对应哪个词/子词/聚合块；再用该视频原词时间映射到秒。确认配套视频是完整原片还是截取片段，保存剪辑起点偏移与来源，防止时间虽未越界却定位到另一片段。原视频重新提取的信息只服务证据展示，不替换预测输入。
3. 若通过text_bert重建，逐条核对token ID、特殊词元、截断/合并规则；词元匹配不等于三模态行映射已证明，后者仍需要处理说明或可核验依据。
4. 对每个选中区间输出原文片段、真实音频时间范围、代表帧时间。一个索引区间对应多个不连续时间段时保留时间段数组，不合并成包含无关内容的大段。
5. 分别记录verified/approximate/unresolved。没有来源的秒数填null；近似定位不得写成精确定位。
6. 对每条提交的关键证据执行机器可回读检查：对应视频存在、时间在有效范围内、文本原文可查、帧能解码。典型解释卡逐条人工回看；全量存在不一致时列出受影响ID并修正映射来源。
7. 写 `q3_predictions_explanations.csv` 和 `q3_explanations.jsonl`，按Spec列名保留所有预测、曲线、证据与来源。最终无标签示例不标“预测正确”。

**通过条件：** 两个专项集所有ID都有结果；附件4提交的原始证据有可核验对应。若仍有核心映射unresolved，报告具体ID/原因/缺失依据，不用自编秒数完成表格。

### E15｜图表、复现检查与最终打包

1. 所有图表从已保存CSV/JSON生成，禁止手工改数字。输出模型比较表、90主情形规律图、位置对比、TAV/异步压力表、混淆矩阵、解释对照图和100条提取汇总。
2. 每个图表旁存对应数据文件与生成配置，标注来自valid还是test、是否3seed均值、误差条含义和样本数。专项集只展示无标签预测汇总。
3. 用新进程重新载入精简包中的权重与统计量，对固定20条valid输入复算；同设备float32默认绝对容差1e-6。跨设备差异单独登记，不以跨设备逐位相同作为唯一复现标准。
4. 运行Spec的A01–A15验收，逐项保存 PASS/FAIL/BLOCKED、证据文件和异常原因；没有执行的项目不能填PASS。
5. 检查提交目录：Q1全部100条特征与映射、核心代码、环境/配置/运行说明、唯一模型权重、两份专项CSV、解释明细。去除优化器状态、多个seed、缓存、重复图片与个人路径。
6. 检查总包≤50,000,000字节，目标≤45MB；超过时优先去除重复和非必要临时材料，不削减必须样本或结果。检查文档、图像和元信息中的姓名、单位、编号。
7. 用交付说明在干净目录重建依赖并完成两类预测入口的复算检查；通用大模型权重的下载/离线使用条件按实际竞赛规则说明，不假称所有大权重已包含。

**输出：** `final_tables/`、`final_figures/`、`acceptance.csv`、`reproduction_report.md`、最终提交包及其文件清单/校验值。

**通过条件：** 所有必须项目有证据；输入ID全覆盖；证据定位未伪造；总包合规；已记录如何重新得到结果。

## 三、把操作转为 agent 的任务

每个步骤交给agent时，使用以下任务结构，填入本文的步骤编号，不要求agent自行重新设计实验：

```text
执行步骤：E__
依据：执行Spec v1.0；逐步实验方案 v1.0；实验默认配置.json
前置证据：该步骤前置产物与通过记录
输入：具体附件路径、只读清单、数据/配置版本
工作：实现并完成该步骤中的编号操作
输出：该步骤列出的文件与逐项检查记录
边界：不执行尚未满足前置条件的后续步骤，不更改冻结数据划分
结束：报告产物位置、检查结果、失败原因及影响范围；不能把计划写成已执行
```

建议把后续项目入口按用途实现为“审计、提取、生成场景、训练、评价、解释、导出、验收”八类；入口必须能显式选择配置和输入路径。本文未交付训练程序，因此没有虚构可以立即运行的命令。

## 四、预算如何计算，哪些可以省略

默认成本分为四块：

| 项目 | 数量 | 估计方式 |
|---|---:|---|
| 核心训练 | 24次 | 各结构每epoch训练+7视图验证耗时 × 实际epoch × 3seed |
| 训练后完整valid评价 | 24×115个情形视图 | 实测单次valid推理耗时，不重新训练 |
| 问题1提取 | 全部100条 | 根据5条试跑的时长与处理速度估算，另加人工核验 |
| 问题3解释 | 全部附件4，另加最多100条valid | 最多163次前向/样本，valid随机对照另计，按实测批处理速度估算 |

提前停止常使训练少于60epoch，但不能把尚未发生的提前停止当保证。为特征失败重跑、解释映射、最终打包保留总可用时间的至少20%；这只是安排建议，不是实测需求。

**默认推荐：24次核心训练，不调参、不复现复杂论文模型。** 优点是对照完整、成本最可控；缺点是可能未达到该结构最优精度。

如果实测预算富余，可选增强最多增加12次，合计36次。若要进行Spec中的4组参数搜索，须在正式批量运行前选定该路线，核心变为48次、含可选最多60次；不能跑完默认路线后仍声称“只花24次”。

不能省略：原始数据核验、掩码正确性、三分类Neutral、同场景公平比较、全量专项输出、Q1全量100条、Q4证据来源与最终体积检查。可以依次取消：M4、M3、M2、MulT、额外图表。减少seed会降低结论等级，必须明确说明。

## 五、当天执行记录怎么写

只记录事实，每条至少包含：

```text
步骤/运行ID：
输入与配置版本：
实际完成内容：
输出文件：
检查结果及证据：
耗时/内存：
失败或阻断项：
可继续的下一步：
```

开始实施时，第一项实际工作是E00/E01；不需要追加更多论文才能开始。得到数据并完成这些检查后，再按本手册进入提取和训练。所有参考工具与论文依据沿用Spec第15节，本文增加的是执行细节，不增加已经验证性能的主张。
